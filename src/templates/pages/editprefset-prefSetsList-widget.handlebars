<div id="contexts-area" class="callout">
    <h5>{{{messageHelper "gpii_pmt_contextsHeader"}}}</h5>
    <table class="table-scroll">
        <thead>
            <tr>
                <th>{{{messageHelper "gpii_pmt_contextIDHeader"}}}</th>
                <th>{{{messageHelper "gpii_pmt_nameHeader"}}}</th>
                <th></th>
                <th></th>
            </tr>
        </thead>
        <tbody>
        {{#each contextNames}}
            <tr>
                <td>{{this}}</td>
                <td>{{{lookup (lookup ../flatPrefs/contexts this) "name" }}}</td>
                <td><button data-contextId="{{this}}" type="button" class="pmt-edit-context button">{{{messageHelper "gpii_pmt_prefsList_editButton"}}}</button></td>
                <td><button data-contextId="{{this}}" type="button" class="pmt-delete-context alert button">{{{messageHelper "gpii_pmt_prefsList_deleteButton"}}}</button></td>
            </tr>
        {{/each}}
        </tbody>
    </table>
</div>
