<div class="row">
    <div class="column medium-12">
        <h2>Selected Demo Sets</h2>
    </div>
</div>
<div class="row">
    {{#each selectedDemoSets}}
        <div class="column medium-4">
            <div class="card">
                <div class="card-divider">
                    <a href="/editprefs/{{@key}}">{{@key}}</a>
                </div>
                {{{md this.doc}}}
            </div>
        </div>
    {{/each}}
</div>
